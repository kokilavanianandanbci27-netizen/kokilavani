#include <stdio.h>
#include <stdlib.h>
#include <math.h>

struct triangle {
    int a;
    int b;
    int c;
};

typedef struct triangle triangle;

// Function to calculate area squared to avoid sqrt overhead
double calculate_area(triangle t) {
    double p = (t.a + t.b + t.c) / 2.0;
    return p * (p - t.a) * (p - t.b) * (p - t.c);
}

int compare(const void* a, const void* b) {
    double area1 = calculate_area(*(triangle*)a);
    double area2 = calculate_area(*(triangle*)b);
    if (area1 < area2) return -1;
    if (area1 > area2) return 1;
    return 0;
}

void sort_by_area(triangle* tr, int n) {
    qsort(tr, n, sizeof(triangle), compare);
}

int main() {
    int n;
    if (scanf("%d", &n) != 1) return 0;
    triangle* tr = malloc(n * sizeof(triangle));
    for (int i = 0; i < n; i++) {
        scanf("%d%d%d", &tr[i].a, &tr[i].b, &tr[i].c);
    }
    sort_by_area(tr, n);
    for (int i = 0; i < n; i++) {
        printf("%d %d %d\n", tr[i].a, tr[i].b, tr[i].c);
    }
    free(tr);
    return 0;
}
